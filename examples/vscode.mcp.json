{
  "servers": {
    "kling": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/kling-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
